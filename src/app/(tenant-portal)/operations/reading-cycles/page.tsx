import { ModulePlaceholder } from "@/components/tenant/module-placeholder";

export default function ReadingCyclesPage() {
  return (
    <ModulePlaceholder
      title="Reading Cycles"
      description="Define and manage meter reading cycles. Lock readings after period close."
      moduleName="Operations"
    />
  );
}
