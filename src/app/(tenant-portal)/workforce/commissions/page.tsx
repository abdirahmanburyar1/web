import { ModulePlaceholder } from "@/components/tenant/module-placeholder";

export default function CommissionsPage() {
  return (
    <ModulePlaceholder
      title="Commissions"
      description="Commission policies and calculation engine (per reading, per collection, bonuses)."
      moduleName="Workforce"
    />
  );
}
